{
    "configurations": [
        {
            "name": "Test Uvicorn",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/sb-env/Scripts/uvicorn.exe",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "args": [
                "main:app",
                "--reload",
                "--host",
                "0.0.0.0",
                "--port",
                "8080"
            ]
        }
    ]
}